# Handoff — 2026-07-11, Session-Ende (Kern 07: Phase B Item 1)

## Aktueller Stand
NumType (Forschungsprojekt: typsichere n-dim Arrays — TS-Typ-Ebene + from-scratch Rust/WASM-Kerne). Remote: `github.com/crankfunk/numtype`, privat. **Phase A komplett** (siehe voriger Handoff-Stand in git history), **Phase B Item 1 = Kern 07 gelandet und unabhängig verifiziert (CONFIRMED)**:

- **Kern 07 — Elementwise-Familie + Vektor-Ops:** `sub`/`mul`/`div` (broadcasting, exakte `add`-Spiegel) + `dot`/`norm`/`cosineSimilarity` auf BEIDEN Surfaces (naive `NDArray`-Referenz + resident `WNDArray`). Rust: ein generischer strided Elementwise-Kernel (kernels/elementwise.rs, Zeile-für-Zeile-Spiegel von `add_strided`, monomorphisiert) + zwei Reduktions-Kernel (kernels/vector.rs: `dot_strided` mit einer aufsteigenden Akkumulator-Kette, `norm_sq_strided` in logischer Row-major-Ordnung) hinter fünf APPENDETEN ABI-Einstiegspunkten (`nt_{sub,mul,div}_strided`, `nt_dot_strided`, `nt_norm_sq_strided`; keine neuen Statuscodes, keine cfg-Gates). **Design-Ökonomie (KB-getragen): `norm`/`cosineSimilarity` haben KEINE eigenen Kernel** — `sqrt`/`*`//` sind IEEE-exakt JS ⇄ WASM, also gepinnte TS-seitige Skalar-Kompositionen über den zwei Reduktionen; Bit-Identität reduziert sich auf die Reduktionen.
- **API-Entscheidungen (dokumentiert, kern-07-ergebnisse.md):** dot/norm/cosine geben **plain `number`** zurück (bewusste Asymmetrie zu `sum()` → `NDArray<[]>`: chainbare Reduktion vs. skalare Konsumenten-Ops; NumPy-Präzedenz); `norm()` = Frobenius/L2 über ALLE Elemente, jeder Rank (löst nebenbei das Guard-Problem niladischer Methoden); `div`/cosine pure IEEE (±Inf/NaN, keine Epsilon-Guards).
- **Typ-Ebene:** neuer `DotCheck<S,B,Op>`-Guard (spike/src/vector.ts) — Fehler AM Argument, Message-Stems wortgleich Runtime ⇄ Compile (Reihenfolge rank-1.-Operand → rank-2. → Länge); Union-DIMS degradieren zu no-claim (nutzt bewusst NICHT das vorbelastete `DimEq`; `IsUnion` aus slice-literal.ts jetzt exportiert). `NDArrayView` unverändert 3 Member.

Gate-Stand (implementiert + vom Verifier unabhängig reproduziert): `pnpm check` clean 0,5–0,68 s · `check:diag` **200 714** (neuer Pin; 3× identisch beim Verifier; Δ +12 336 ggü. 188 378 — Spec-Erwartung „low thousands" verfehlt, absolutes Gate ≤250k klar PASS, ehrlich im Ergebnisdoc) · `test:core` 817 · `test:resident` **3092+2** (+772: 454 elementwise + 318 vector) · `cargo test` **157** (Baseline war real 109, nicht dokumentierte 110 — Doku-Drift, in CLAUDE.md korrigiert) · `test:threaded` 65 · `pnpm demo` all-agree inkl. neuer Embedding-Sektion · `bench:editor` hard gate PASS. **Neuer Artefakt-Pin:** `7a65d800…` (voll in CLAUDE.md/kern-07-ergebnisse.md; Export-addierende Phase ändert den Hash legitim — Freeze-Beweis vierteilig, siehe CLAUDE.md Frozen-baseline-Abschnitt). Drei Mutations-Beweise (div `/`→`*`: 151/454 rot; dot `+=`→`-=`: 201/318 rot; Verifier-eigener mul `*`→`+`: 150/454 rot; alle Reverts grün, Hash restauriert).

## In dieser Session erledigt
- Kern 07 komplett nach Phasen-Disziplin: bindende Spec (docs/kern-07-elementwise-vector-spec.md, mit vorregistrierten ABSOLUTEN Gates) → deep-Implementierung (eine offengelegte Abweichung: TS-Klassenkörper mit privaten Konstruktoren nehmen INSERTION-only-Diffs statt Literal-EOF-Append — vom Verifier als intent-erhaltend bestätigt; als Regel in CLAUDE.md verewigt) → Fresh-Context-Verify (CONFIRMED; alle Gates selbst reproduziert, eigener Mutant) → Ergebnisdoc mit Post-Verification-Addendum → KB-Capture → Commit.
- FOLLOWUPS: Union-Guard-Latenz als ZWEI-Facetten-Item eingetragen (a: Union-Dims in DimEq/CompatDim, beim Spec-Design entdeckt; b: Union-of-WHOLE-SHAPES unterläuft den argument-seitigen `Guard` — vom Verifier EMPIRISCH auch auf dem vorbestehenden `add` reproduziert; systemisch, bewusst nicht nebenbei gefixt).
- CLAUDE.md: Kern 07 in Done, neue Pins (check:diag 200 714, resident 3092+2, cargo 157, Artefakt-Hash), Freeze-Disziplin um die Export-addierende-Phase-Regel + Insertion-only-Regel erweitert; roadmap.md Phase-B-Statusblock.

## Offen / in Arbeit
Nichts halbfertig.

## Nächste Schritte (priorisiert, = Roadmap Phase B Rest)
1. **Runtime-`reshape`/`flatten`** — konsumieren das fertige `LiteralShapeProduct` (Spike 04); DABEI die FOLLOWUPS-Messobligation einlösen: Editor-Hover-Kosten echter `reshape()`-Sites (bench:editor-Workload erweitern) + Guard-Message-Wortlaut in der Spec festlegen.
2. **`keepdims` im Runtime-`sum()`** (Typ-Ebene existiert, getestet).
3. **Spezialwerte im Differential-Generator** (NaN/±Inf/±0/Subnormals) — der „bit-identisch"-Anspruch ist weiterhin nur für normale endliche Werte + gezielte Fixtures belegt (Kern 07 hat div-by-zero/±0/NaN-Fixtures, aber keine systematische Injektion). Comparator-Standard beachten: `Object.is` = NaN-Payload-insensitiv (ehrlich dokumentiert, WASM-NaN-Payload-Nichtdeterminismus).
4. Perf-Kür nur nach Messung (Packing-Reuse, SIMD elementwise — memory-bound, erst messen); Kür/klein: non-integer start/stop-Guard, Deep-readonly-shape-Entscheidung, Union-Guard-Scheibe (FOLLOWUPS a+b zusammen).

## Bekannte Probleme / Stolperfallen
- **Neu (Kern 07):** Export-addierende Phasen ändern den Artefakt-Hash legitim — Freeze-Beweis vierteilig (alter Pin reproduziert VOR Edits → zero-diff/append-only-`cmp` → Verhaltens-Pins → neuer Pin); TS-Klassenkörper = insertion-only, nie Literal-Append (private Konstruktoren); künftige Specs sagen pro Datei, welche Disziplin gilt. `backend-oom.test.ts` hält einen handgeschriebenen `CoreExports`-Mock — JEDE neue ABI-Signatur braucht dort einen `notImplemented`-Stub (mechanisch, aber leicht zu vergessen). check:diag zeigte beim Implementierer EINEN Lauf mit 199 974 vs. sonst stabil 200 714 — unrepliziert, als One-off notiert.
- **Union-Guard-Latenz (FOLLOWUPS, zwei Facetten):** siehe oben — bei künftigen Guard-Typen Union-Dims UND Union-of-Shapes von Anfang an filtern (Kern 07 `DotCheck` = Vorbild für Dims).
- **Unverändert gültig:** alle Einträge des vorigen Handoffs (Typ-Ebenen-Regeln Spikes 03–06, Threads-Build-Regeln, cargo-Config CWD, Test-Explizitlisten mit Guard, TS7-Ein-Diagnose-Verhalten, `normalizeSliceSpecs`-Blind-Spot — jetzt analog auch `assertVectorPair` als dokumentierter shared-Validator-Blind-Spot).

## Wichtige Dateien & Befehle
- **Specs & Ergebnisse:** `docs/{spike-01..06,kern-01..07}-*.md` · `docs/roadmap.md` (Phase-A- + Phase-B-Statusblöcke) · Backlog `FOLLOWUPS.md`.
- **Code:** Typ-Ebene `spike/src/` (`vector.ts` NEU = DotCheck; `slice-literal.ts` = Digit-Arithmetik + exportiertes `IsUnion`; `ndarray.ts`/`wasm/resident.ts` = Klassen mit je 6 neuen Ops); Kernel `crates/core/src/kernels/{elementwise,vector}.rs` NEU; ABI-Appends in `abi.rs`.
- **Befehle (alle vom Repo-Root):** `pnpm check`/`check:diag` (Pin: 200 714) · `pnpm test:core` (817) · `pnpm test:resident` (3092+2; +`:gc`) · `pnpm test:threaded` (65, nightly!) · `pnpm demo` · `pnpm bench:{scaling,chain,strided,blocked,slice,threaded,crossover,editor}` · `cargo test --manifest-path crates/core/Cargo.toml` (157).
