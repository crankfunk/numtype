# Handoff — 2026-07-11, Session-Ende (Spikes 04–06: Phase A komplett)

## Aktueller Stand
NumType (Forschungsprojekt: typsichere n-dim Arrays — TS-Typ-Ebene + from-scratch Rust/WASM-Kerne). Alles committet und gepusht (`main` = `origin/main` = `411e138` vor dieser Handoff-Runde; Remote: `github.com/crankfunk/numtype`, privat). **Roadmap-Phase A ist KOMPLETT, inkl. Kür** — das strukturelle Killer-Risiko (Editor-Erlebnis) ist ausgeräumt, alle Typ-Ebenen-Grundsatzentscheidungen sind gefallen. In dieser Session gelandet:

- **Spike 04 — Typ-Ebenen-Shape-Produkte** (`LiteralShapeProduct<S>`, Schulbuch-Multiplikation über Digit-Strings in slice-literal.ts): 177/177-BigInt-Paritäts-Grid; never-wrong per Grenz-Filter (Union/never) + MAX_SAFE_INTEGER-Kappe (größere Digit-Strings würden durch `${infer N extends number}` zu FALSCHEN Literalen doppel-runden). **ENTSCHEIDUNG: GO als offengelegte Abweichung von der vorregistrierten Gate-Regel** (G1 1,0321× vs. ≤1,02× und G2-Mean gerissen; Absolut-/Editor-Gates klar PASS; Verify-Zweitmeinung „defensible"; von Marvin explizit akzeptiert). Neuer empirischer TS7-Befund: nie referenzierte template-literal-lastige Aliase kosten DEKLARATIONS-Instantiations (~2k/Spike, dreimal beobachtet).
- **Spike 05 — Varianz-Design (A3, vor API-Freeze):** `NDArray<S>` bleibt invariant (two-of-three), impliziter Top-Typ = neue minimale kovariante Read-View **`NDArrayView<out S>`** (nur shape/strides()/toNestedArray(); ohne data für Backend-Portabilität; ohne shape-berechnende Member — TS2636, die `out`-Prüfung ist ABSTRAKT); Klasse `implements NDArrayView<S>`; `AnyNDArray` bleibt dokumentiert-unsicherer Escape-Hatch; Demo-printArray migriert. Verify-Fund als Hausregel verewigt: **Method-Shorthand-Parameter prüft TS BIVARIANT** — die Annotation fängt ein künftiges Shorthand-Member mit S-Argument NICHT; Funktions-Member nur property-style.
- **Spike 06 — Phase-A-Kür:** Range-Slices berechnen Dims statisch auch für **negative literale start/stop** (KEINE vorzeichenbehaftete Addition nötig — Compare+MultiSub+Clamp, zweite Bestätigung „Vergleichen statt Rechnen") und **literale steps ≥ 2** (neue Schulbuch-ceil-Division `DivCeil`); provably-invalide steps (0, negativ, Dot-Form ±1.5) sind Compile-Fehler am Argument (Runtime-Stem wortgleich). **280/280-Grid mit der IMPORTIERTEN echten `normalizeSliceSpecs` als Ground Truth** (Generator prüft eigene Invarianten); 32/32 unabhängige Verifier-Proben; Guard-Nicht-Vakuität per Neutering-Beweis.

Gate-Stand (zuletzt gelaufen, diese Session): `pnpm check` clean · `check:diag` **188 378** Instantiations (= 3,77 % des ~5M-Budgets; Stufen-Pins: 133 656 → Spike 04 → 133 727 → Spike 05 → 188 378 nach Spike 06) · `test:core` 817 · `test:resident` **2320+2** (+1 Spike-06-Paritätstest) · `pnpm demo` all-agree · `bench:editor` hard gate PASS · Wall-Clock `pnpm check` ~0,42 s flach. NICHT gelaufen (keine Rust-/Threads-Änderungen in der ganzen Session): `test:threaded`, cargo-Tests, Artefakt-Hash-Check — Baseline unberührt per null Rust-Diffs.

## In dieser Session erledigt
- Spike 04 komplett: Spec → deep-Implementierung → 177er-Grid → Verify (CONFIRMED, 4 Argument-Korrekturen übernommen, Worst-Case ehrlich 15 % statt 8 %) → Ergebnisdoc → Commit 3829952.
- Spike 05 komplett: 8 eigene Kovarianz-Proben → Spec → deep → Verify (CONFIRMED mit Bivarianz-Widerlegung) → Commit ffd041d. Streunender Kern-06-Worktree-Branch aufgeräumt (PoCs als Tarball im Session-Scratchpad gesichert, /private/tmp — vergänglich).
- Spike 06 komplett: Spec → deep (eine notwendige Abweichung: 5 alte Grenz-Pins intent-erhaltend re-formuliert) → Review-Fix (−1.5 sign-agnostisch) → 280er-Runtime-Grid → Verify (CONFIRMED; −1e5-Kommentar-Beispiel korrigiert) → Commit 411e138.
- KB: neue Notiz `budget-gates-vorregistrieren-absolute-statt-schaetzung`; Nachträge in `typ-ebenen-arithmetik-digit-strings` (Multiplikation, Division, Template-Form-Fakten), `ts7-native-compiler-empirie` (Deklarationskosten), `ts-varianz-two-of-three-bei-typlevel-guards` (abstrakte Annotation, Bivarianz-Loch), `runtime-fehler-auf-typ-ebene-heben` (importierte Ground Truth, Neutering-Beweis); MOC verdrahtet, Graph gebaut, Kanten verifiziert.
- CLAUDE.md: Spikes 04–06 in Done, Phase-B-Next, neue Gotcha-Bullets; FOLLOWUPS: 3 Austräge, 5 neue Items; roadmap.md: Phase-A-Status-Block.

## Offen / in Arbeit
Nichts halbfertig. HANDOFF/roadmap-Änderungen dieser Handoff-Runde sind beim Lesen dieser Notiz committet.

## Nächste Schritte (priorisiert, = Roadmap Phase B)
1. **Elementwise-Familie (sub/mul/div)** + **dot/Norm/Cosine-Similarity** — der Embedding-Use-Case; Typ-Ebene existiert weitgehend (Broadcast), Runtime-Kerne + Differentialtests neu.
2. **Runtime-`reshape`/`flatten`** — konsumieren das fertige `LiteralShapeProduct` (Spike 04); DABEI die FOLLOWUPS-Messobligation einlösen: Editor-Hover-Kosten echter `reshape()`-Sites (bench:editor-Workload erweitern) + Guard-Message-Wortlaut festlegen (Phase-B-Spec).
3. **`keepdims` im Runtime-`sum()`** (Typ-Ebene existiert, getestet).
4. **Spezialwerte im Differential-Generator** (NaN/±Inf/±0/Subnormals) — der „bit-identisch"-Anspruch muss auch dort belegt sein; transzendente Ops erst nach Determinismus-Entscheidung.
5. Kür/klein: symmetrischer non-integer-start/stop-Guard (Dot-Form-Einsicht aus Spike 06 trägt); Deep-readonly-`shape`-Entscheidung (FOLLOWUPS, berührt Clean-Hover-Regel).

## Bekannte Probleme / Stolperfallen
- **Typ-Ebene (neu, Spikes 04–06):** Produkte bei MAX_SAFE_INTEGER kappen (sonst falsche Literale); Union/never-Dims an der GRENZE filtern, wenn das Verdikt ein unbeschränkter WERT ist (Subset-Checks nur bei endlichem Verdikt-Alphabet); `out`-Annotation prüft ABSTRAKT (faktisch monotone Typ-Funktionen scheitern trotzdem — TS2636) UND hat das Method-Shorthand-Bivarianz-Loch (Hausregel in ndarray.ts); Template-Form: plain digits bis exkl. 1e21, Dot-Form = beweisbarer Nicht-Integer (sign-agnostisch); `nd.shape[0] = 99` typechecked (vorbestehend, FOLLOWUPS-Entscheidung).
- **Prozess-Lehren (verbindlich, in CLAUDE.md):** Budget-Gates auf ABSOLUTES legen, Messwert wird Pin (nie Schätzung der Messgröße gaten; Stress-Proben nie in gegateten Mean mischen — Spike-04-Lektion); wer eine Subset-Grenze verschiebt, plant die Re-Formulierung der alten Grenz-Pins ein (Spike-06-Lektion); Paritäts-Grids importieren die echte Ground Truth statt sie nachzubauen, mit selbstprüfenden Generator-Invarianten.
- **Unverändert gültig:** Freeze-Disziplin (abi.rs/matmul_blocked.rs/shape.rs append-only, Beweis = Artefakt-Hash aus Clean-Rebuild); Threads-Build nur gepinnter nightly-2026-07-09 (+rust-src), env RUSTFLAGS ERSETZT config-Flags (`+simd128` mitführen), `thread_local!` verboten, Views nie cachen; Threads-Tests pinnen Routen explizit; cargo-Config CWD-basiert → alles vom Repo-Root; Test-Explizitlisten mit Guard; `normalizeSliceSpecs`-Differential-Blind-Spot; TS7 zeigt EINE Diagnose pro Call/Pass.

## Wichtige Dateien & Befehle
- **Specs & Ergebnisse:** `docs/{spike-01..06,kern-01..06}-*.md` · `docs/roadmap.md` (Phase-A-Status-Block) · `docs/wettbewerbsanalyse-und-usp.md` · Backlog `FOLLOWUPS.md`.
- **Code:** Typ-Ebene `spike/src/` (`slice-literal.ts` = gesamte Digit-Arithmetik: Sub/Compare/Mul/DivCeil + `LiteralRangeDim`/`LiteralIndexBounds`/`LiteralShapeProduct`/`LiteralStepInvalid`; `slice.ts` = Guards; `ndarray.ts` = Klasse + `NDArrayView<out S>` + `AnyNDArray`); WASM `spike/src/wasm/` (v1 FROZEN, resident, threaded mit Auto-Weiche); DX-Harness `spike/bench-dx/`; Rust `crates/core/`.
- **Befehle (alle vom Repo-Root):** `pnpm check`/`check:diag` (Pin: 188 378) · `pnpm test:core` (817) · `pnpm test:resident` (2320+2; +`:gc`) · `pnpm test:threaded` (65, nightly!) · `pnpm demo` · `pnpm bench:{scaling,chain,strided,blocked,slice,threaded,crossover,editor}` · `cargo test --manifest-path crates/core/Cargo.toml` (110).
